services:
  api:
    build: ./api
    ports:
      - "3000:3000"
    env_file:
      - .env

  db:
    image: postgres:15
    env_file:
      - .env
    volumes:
      - db-data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql

volumes:
  db-data: